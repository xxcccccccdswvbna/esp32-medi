# =============================================================================
# Dev3: 标识 48,80,27,70,00,00 | 只读传感器（状态由 ble.yaml 解析更新）
# =============================================================================
# 🔹 仅需声明传感器组件，无需 select/button（无控制指令）

